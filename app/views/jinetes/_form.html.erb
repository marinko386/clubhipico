<%= form_for(@jinete) do |f| %>
  <% if @jinete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jinete.errors.count, "error") %> prohibited this jinete from being saved:</h2>

      <ul>
      <% @jinete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
